fastapi==0.113.0
uvicorn[standard]==0.30.3
httpx==0.27.0
supermarktconnector==0.8.1
python-dotenv==1.0.1 # Good practice for potential future env vars

# Remove Flask/Gunicorn specific ones if they exist
# flask
# flask-cors
# gunicorn 